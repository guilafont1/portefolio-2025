{% extends 'base.html.twig' %}

{% block title %}Accueil – Julien Font{% endblock %}

{% block body %}

<section class="py-5" style="padding-top: 80px;">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-7">

                <h1 class="mb-4">
                    Développeur full-stack
                </h1>

                <p class="mt-3 mb-4" style="max-width: 620px;">
                    Je développe des applications web, principalement avec Symfony, en privilégiant
                    l'architecture, la maintenabilité et la fiabilité. Je reste ouvert aux nouvelles
                    technologies et aux approches innovantes. Actuellement en alternance chez KOOREO.
                </p>

                <div class="d-flex gap-3 mt-4">
                    <a href="{{ path('projects') }}" class="btn btn-outline-dark px-4">
                        Voir les projets
                    </a>
                    <a href="{{ path('contact') }}" class="btn btn-outline-secondary px-4">
                        Contact
                    </a>
                </div>

            </div>
            <div class="col-lg-5 mt-4 mt-lg-0">
                <div class="text-center">
                    <img src="{{ asset('images/profile.jpg') }}" 
                         alt="Julien Font" 
                         class="img-fluid rounded"
                         style="max-width: 280px; width: 100%; height: auto; object-fit: cover;">
                </div>
            </div>
        </div>
    </div>
</section>

<section class="bg-light" style="padding-top: 0.5; padding-bottom: 3rem;">
    <div class="container">
        <div class="row">
            <div class="col-lg-10">

                <h2 class="mb-4" style="margin-top: 0;">À propos</h2>

                <p>
                    Étudiant en Master Expert en informatique et systèmes d'information.
                </p>

                <p>
                    Je conçois des applications web et des API, j'intègre des données et j'adapte les outils aux besoins du projet.
                </p>

                <p class="mb-0">
                    J'accorde de l'importance aux architectures claires, testables et maintenables,
                    avec une attention constante à la qualité et à la fiabilité.
                </p>

            </div>
        </div>
    </div>
</section>

<section style="padding-top: 0.5; padding-bottom: 3rem;">
    <div class="container">
        <div class="row">
            <div class="col-lg-10">
                <h2 class="mb-4" style="margin-top: 0;">Études</h2>

                {% for edu in education %}
                    <div class="mb-4">
                        <div class="d-flex justify-content-between align-items-start mb-2">
                            <div>
                                <h5 class="mb-1" style="font-family: 'IBM Plex Mono', monospace; font-weight: 500;">
                                    {{ edu.degree }}
                                </h5>
                                <p class="text-muted mb-1">
                                    {{ edu.school }} • {{ edu.location }}
                                    {% if edu.company %}
                                        <span class="text-muted">({{ edu.type }} chez {{ edu.company }})</span>
                                    {% else %}
                                        <span class="text-muted">({{ edu.type }})</span>
                                    {% endif %}
                                </p>
                            </div>
                            <span class="badge status-badge status-{{ edu.current ? 'ongoing' : 'completed' }}">
                                {% if edu.current %}
                                    En cours
                                {% else %}
                                    {{ edu.start_date }}{% if edu.end_date %} - {{ edu.end_date }}{% endif %}
                                {% endif %}
                            </span>
                        </div>
                        {% if edu.description %}
                            <p class="text-muted mb-0">{{ edu.description }}</p>
                        {% endif %}
                    </div>
                {% endfor %}

            </div>
        </div>
    </div>
</section>

<section class="bg-light" style="padding-top: 0.5; padding-bottom: 3rem;">
    <div class="container">
        <h2 class="mb-4" style="margin-top: 0;">Projets sélectionnés</h2>

        <div class="row">
            {% for project in projects|slice(0,3) %}
                <div class="col-md-6 col-lg-4 mb-4">
                    <div class="card h-100">
                        <div class="card-body">
                            <h5 class="card-title">{{ project.title }}</h5>
                            <p class="text-muted small">
                                {{ project.description }}
                            </p>
                            <a href="{{ path('project_detail', { slug: project.slug }) }}"
                               class="text-decoration-none">
                                Voir le projet →
                            </a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>

        <a href="{{ path('projects') }}" class="d-inline-block mt-3">
            Voir tous les projets →
        </a>
    </div>
</section>

<section style="padding-top: 0.5; padding-bottom: 3rem;">
    <div class="container">
        <div class="row">
            <div class="col-lg-10">
                <h2 class="mb-4" style="margin-top: 0;">Expérience</h2>

                {% for experience in experiences %}
                    <div class="mb-4">
                        <div class="d-flex justify-content-between align-items-start mb-2">
                            <div>
                                <h5 class="mb-1" style="font-family: 'IBM Plex Mono', monospace; font-weight: 500;">
                                    {{ experience.title }}
                                </h5>
                                <p class="text-muted mb-1">
                                    {{ experience.company }} • {{ experience.location }}
                                    <span class="text-muted">({{ experience.type }})</span>
                                </p>
                            </div>
                            <span class="badge status-badge status-{{ experience.current ? 'ongoing' : 'completed' }}">
                                {% if experience.current %}
                                    En cours
                                {% else %}
                                    {{ experience.start_date }}{% if experience.end_date %} - {{ experience.end_date }}{% endif %}
                                {% endif %}
                            </span>
                        </div>
                        {% if experience.description %}
                            <p class="text-muted mb-2">{{ experience.description }}</p>
                        {% endif %}
                        {% if experience.project_slug is defined and experience.project_slug %}
                            <a href="{{ path('project_detail', {slug: experience.project_slug}) }}" 
                               class="text-decoration-none" 
                               style="color: var(--accent);">
                                Voir le projet →
                            </a>
                        {% endif %}
                    </div>
                {% endfor %}

            </div>
        </div>
    </div>
</section>

{% endblock %}
